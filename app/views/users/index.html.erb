<P> users index</p>
<div >
<%= link_to 'New User',  new_user_registration_path, method: :get, class: "btn btn-success btn-lg float-right" %>
</div>

<table class="table table-hover">
      <tr class="p-3 mb-2 bg-secondary text-white">
        <th>Id</th>
        <th>Email</th>
        <th>Admin</th>
      </tr>
    </thead>
    <tbody>
      <% @user.each do |user| %>
        <tr>
          
          <td><%= user.id%></td>
          <td><%= user.email %></td>
          <td><%= user.admin %></td>
          
         
        </tr>
      <% end %>
    </tbody>
  </table>
 
 
